name: Generate All Location Forecasts

on:
  schedule:
    - cron: '0 */6 * * *'  # Každých 6 hodín
  workflow_dispatch:  # Alebo ručne

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          pip install cdsapi requests xarray cfgrib pandas numpy

      - name: Create CDS config
        run: |
          echo "url: https://cds.climate.copernicus.eu/api" > ~/.cdsapirc
          echo "key: e1f7a29a-8a37-494c-86ff-8795fea9b426" >> ~/.cdsapirc

      - name: Generate forecasts for all locations
        run: |
          cd backend
          
          # Načítaj všetky lokality zo súboru locations.json
          python -c "
import json

with open('locations.json', 'r', encoding='utf-8') as f:
    data = json.load(f)
    locations = data.get('locations', [])
    
print(f'Found {len(locations)} locations to process')

# Vygeneruj JSON pre každú lokalitu
import subprocess
for loc in locations:
    name = loc['name']
    lat = loc['lat']
    lon = loc['lon']
    print(f'Generating: {name}')
    subprocess.run(['python', 'fetch_cds_ecmwf.py', name, str(lat), str(lon)], check=True)
"

      - name: Commit and push changes
        run: |
          git config --local user.email "action@github.com"
          git config --local user.name "GitHub Action"
          git add backend/ecmwf_forecast_*.json
          git diff --staged --quiet || git commit -m "Update location forecasts $(date +'%Y-%m-%d %H:%M')"
          git push
